import React from 'react';
import PropTypes from 'prop-types';
import './../index.css';




/* AI COMP */
const Ai = function(props, calcWinner) {
  const human = props.startSym; 
  const computer = (human === 'X') ? 'O' : 'X';
  

};




export default Ai;
